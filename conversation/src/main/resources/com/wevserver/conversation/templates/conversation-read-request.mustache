<!DOCTYPE html>
<html>
    {{>com/wevserver/ui/templates/head-partial}}
    {{>com/wevserver/ui/templates/header-partial}}
    <body>
        <section class="section">
            <form method="get" action="/conversation/conversation-read">
                <div class="field">
                    <label class="label">Id</label>
                    <div class="control">
                        <input class="input" type="text" name="id"
                        {{#requestParams.id}} value="{{.}}"
                        {{/requestParams.id}} placeholder="Id" />
                    </div>

                    <ul>
                        {{#errorMessages.fieldErrors.id}}
                        <li class="help is-danger">{{.}}</li>
                        {{/errorMessages.fieldErrors.id}}
                    </ul>
                </div>

                <div>
                    <ul>
                        {{#errorMessages.globalErrors}}
                        <li class="help is-danger">{{.}}</li>
                        {{/errorMessages.globalErrors}}
                    </ul>
                </div>

                <div class="control">
                    <button type="submit" class="button is-primary">
                        Submit
                    </button>
                </div>
            </form>
        </section>
    </body>
    {{>com/wevserver/ui/templates/footer-partial}}
</html>
