<!DOCTYPE html>
<html>
    {{>com/wevserver/ui/templates/head-partial}}
    {{>com/wevserver/ui/templates/header-partial}}
    <body>
        <section class="section">
            <form
                method="post"
                action="/conversation/conversation-reply-create"
            >
                <div class="field">
                    <label class="label">>{{#t}}conversation-id{{/t}}</label>
                    <div class="field has-addons">
                        <div class="control is-expanded">
                            <input class="input" type="text"
                            name="conversationId" {{#conversationId}}
                            value="{{.}}" {{/conversationId}}
                            placeholder="{{#t}}conversation-id{{/t}}" />
                        </div>
                        <div class="control">
                            <button
                                type="submit"
                                formaction="{{pickConversationUri}}"
                                formmethod="post"
                                class="button"
                            >
                                {{#t}}find{{/t}}
                            </button>
                        </div>
                    </div>
                    <ul>
                        {{#fieldErrors.conversationId}}
                        <li class="help is-danger">{{.}}</li>
                        {{/fieldErrors.conversationId}}
                    </ul>
                </div>

                <div class="field">
                    <label class="label">{{#t}}details{{/t}}</label>
                    <div class="control">
                        <input class="input" type="text" name="details"
                        {{#details}} value="{{.}}" {{/details}}
                        placeholder="{{#t}}details{{/t}}" />
                    </div>
                    <ul>
                        {{#fieldErrors.details}}
                        <li class="help is-danger">{{.}}</li>
                        {{/fieldErrors.details}}
                    </ul>
                </div>

                <div>
                    <ul>
                        {{#globalErrors}}
                        <li class="help is-danger">{{.}}</li>
                        {{/globalErrors}}
                    </ul>
                </div>

                <div class="control buttons">
                    <button type="submit" class="button">
                        {{#t}}submit{{/t}}
                    </button>
                    <button
                        type="submit"
                        formaction="{{saveUri}}"
                        formmethod="post"
                        class="button"
                    >
                        {{#t}}save{{/t}}
                    </button>
                </div>
            </form>
        </section>
    </body>
    {{>com/wevserver/ui/templates/footer-partial}}
</html>
